(ns dlist-atom.core-spec
;  (:refer-clojure :exclude [])
  (:require [speclj.core :refer :all]
            [dlist-atom.core :refer :all])
;  (:import [dlist-atom.core ])
  )

;; # The Tests
;;
;; We are going to use [spelj](https://github.com/slagyr/speclj) for our tests.



(describe "The DList Declaration"
          )

(describe "The insert at front function"


          ;;(it "should not reorder the list."
          ;;    )

          ;;(it "should not remove any elements."
          ;;    )
          )

(describe "The insert at back function"
          (it "should insert at last.")
          )

(run-specs)





